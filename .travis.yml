language: cpp

matrix:
  include:
    - os: linux
      compiler: gcc
    - os: linux
      compiler: clang
      env: CLANG_STATIC_ANALYZER=true
    - os: linux
      compiler: clang
      env: CLANG_SANITIZERS=true
    - os: osx
      compiler: clang
    - os: osx
      compiler: clang
      env: COVERALLS=true
    
before_install:
  - if [ "$TRAVIS_OS_NAME" == "osx" ]; 
    then 
      brew update > /dev/null 2>&1;
      brew tap homebrew/science;
      brew install --quiet eigen;
      brew install --quiet tbb --c++11;
      brew install --quiet nlopt;
    fi
  - if [ "$TRAVIS_OS_NAME" == "linux" ]; 
    then 
      sudo add-apt-repository --yes ppa:ubuntu-toolchain-r/test;
      sudo add-apt-repository --yes ppa:smspillaz/cmake-3.0.2;
      sudo apt-get update -qq;
      sudo apt-get purge -qq cmake;
      sudo apt-get install -qq cmake libboost-dev libboost-test-dev libeigen3-dev libtbb-dev;
      cmake --version;
    fi
  - if [ "$TRAVIS_OS_NAME" == "linux" ] && [ "$CXX" == "g++" ];
    then
      sudo apt-get install -qq gcc-4.4 g++-4.4 libstdc++6-4.4-dev;
    fi
  - if [ "$TRAVIS_OS_NAME" == "linux" ] && [ "$CXX" == "clang++" ];
    then
      sudo apt-get install -qq gcc-4.8 g++-4.8 libstdc++-4.8-dev clang-3.4;
    fi

before_script:
  - if [ "$CXX" == "g++" ]; 
    then
      export CC=gcc-4.4;
      export CXX=g++-4.4;
    fi
  - if [ -z "$COVERALLS" ]; 
    then
      echo "CHECKING CODE COVERAGE, $COVERALLS"; 
      cmake -DCOVERALLS=ON -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTING=ON .;
    elif [ -z "$CLANG_STATIC_ANALYZER" ];
    then
      echo "RUNNING CLANG STATIC ANALYZER, $CLANG_STATIC_ANALYZER";
      CC=ccc-analyzer CXX=c++-analyzer cmake -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTING=ON -DUSE_THREADS=OFF .;
    elif [ -z "$CLANG_SANITIZERS" ];
    then 
      echo "RUNNING CLANG ADDRESS and UNDEFINED BEHAVIOR SANITIZERS, $CLANG_SANITIZERS";
      cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo -DBUILD_TESTING=ON -DUSE_THREADS=OFF -DWITH_SANITIZERS=ON .;
    else
      if [ "$TRAVIS_OS_NAME" == "osx" ];
      then
        echo "OSX RELEASE BUILD";
        cmake -DBUILD_TESTING=ON -DBUILD_EXAMPLES=ON .; 
      else
        echo "LINUX RELEASE BUILD";
        cmake -DBUILD_TESTING=ON .;
      fi
    fi

script:
  - if [ "$TRAVIS_OS_NAME" == "linux" ] && [ "$CXX" == "clang++" ];
    then
      scan-build make;
    else
      make;
    fi
  - if [ -z "$COVERALLS" ];
    then 
      make coveralls;
    else
      ctest --output-on-failure; 
    fi

notifications:
  email:
    on_success: change
    on_failure: always
